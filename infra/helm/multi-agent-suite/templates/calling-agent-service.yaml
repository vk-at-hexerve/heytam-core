{{- if .Values.agents.calling.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: calling-agent
  labels:
    {{- include "multi-agent-suite.labels" . | nindent 4 }}
    app.kubernetes.io/component: calling-agent
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.agents.calling.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "multi-agent-suite.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: calling-agent
{{- end }}
